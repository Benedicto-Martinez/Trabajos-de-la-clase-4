using System;

class Program
{
    static void Main()
    {
        int count = 0;
        int numero = 0;

        while (count < 5)
        {
            if (numero % 2 == 0)
            {
                Console.WriteLine(numero);
                count++;
            }
            numero++;
        }
    }
}
